#### 代码结构
目录规范，所有代码文件（.py）都放在`src/`目录下，数据文件（.csv）放在`data/`目录下。
运行时，确保在项目根目录下执行，python src/main.py
顶层模块为`main.py`，负责初始化项目、调用其他模块、处理异常。
命名规范清晰，使用驼峰命名，禁止无意义命名
代码格式化，缩进统一，结构清晰
函数和类添加完整注释
必须做参数校验、空值判断和异常处理
代码模块化，单一职责，简洁高效
附带依赖安装命令、使用示例和说明
保证代码可直接运行，无语法错误
说明文件（如`README.md`、`LICENSE`）放在项目根目录下